'use strict';
/* const firstName = 'Tiago';
calcAge(2001);
/* console.log(year); */
/* 
function calcAge(birthYear) {
  const age = 2037 - birthYear;
  function printAge() {
    const output = `${firstName}, you are ${age}, born in ${birthYear}`;
    console.log(output);

    if (birthYear >= 1981 && birthYear <= 1996) {
      const str = `Oh, and you're a millenial, ${firstName}`;
      console.log(str);

      function add(a, b) {
        return a + b;
      }
    }
  }
  printAge();
  return age;
}

let year = 1999; */

/* calcAge(2001); */

/*Hoisting*/

/* var me = 'Jonas';
let job = 'teacher';
const year = 1991; */

/* // EXAMPLE

if (!numProducts) deleteShoppingCart(); // Se não houver produtos apaga mas como declaramos numProducts com var então se chamamos antes da inicialização é um falsy value e o if ira apagar os produtos

var numProducts = 10;

function deleteShoppingCart() {
  console.log('All products deleted!');
}
 */

//THIS
